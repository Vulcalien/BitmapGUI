<h1 align="center">
    BitmapGUI Library
</h1>

A simple Java GUI Library made for videogames. Every input is processed when calling the main panel's `tick()` method.\
Since this is built upon [Bitmap-Utility](https://github.com/Vulcalien/Bitmap-Utility), it's even possible to set single pixels while rendering.
